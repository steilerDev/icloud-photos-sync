
/**
 * Filename of the resource file
 */
export const RESOURCE_FILE_NAME = `.icloud-photos-sync`;
export const RESOURCE_FILE_ENCODING = `utf8`;

/**
 * Possible events for the resource manager
 */
export enum EVENTS {
    NO_RESOURCE_FILE_FOUND = `no-file-find`
}